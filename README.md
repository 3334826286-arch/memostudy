# MemoStudy

MemoStudy is the current active project in this workspace.

## Active project files

- `app/`
- `components/`
- `lib/`
- `public/`
- `supabase/`
- `package.json`
- `package-lock.json`
- `next.config.mjs`
- `netlify.toml`

## Build and preview

```bash
npm run dev
npm run build
```

Netlify build settings:

- Build command: `npm run build`
- Publish directory: `out`

## Workspace cleanup

Archived and non-project items were moved into:

- `_workspace/legacy`
- `_workspace/deploy`
- `_workspace/logs`

These folders contain old exports, deployment packages, unrelated submissions, and historical logs so the project root stays focused on MemoStudy only.
